package com.htnova.<%= module %>.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.htnova.<%= module %>.entity.<%= Entity %>;
import com.htnova.<%= module %>.dto.<%= Entity %>Dto;
import org.apache.ibatis.annotations.Param;

import java.util.List;

public interface <%= Entity %>Mapper extends BaseMapper<<%= Entity %>> {

    IPage<<%= Entity %>> findPage(IPage<Void> xPage, @Param("<%= entity %>Dto") <%= Entity %>Dto <%= entity %>Dto);

    List<<%= Entity %>> findList(@Param("<%= entity %>Dto") <%= Entity %>Dto <%= entity %>Dto);

}
